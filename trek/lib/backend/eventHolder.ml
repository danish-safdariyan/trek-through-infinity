(* module EventHolder = struct let curr_day_cap = 20 (*start with a 20 element
   array*) let lim_event_cap = 10 (*limit to 10 events in a day*)

   (* val find: 'k -> ('k, 'v) t -> 'v option *) end *)
